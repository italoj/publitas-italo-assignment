# Ítalo Araújo Assignment to Publitas.com
A Simple canvas slider made in React to Publitas.com

## Setup

- Clone the project repo
- run `yarn install` or `npm install`
- Then run `yarn start` or `npm start`

## Tests

`yarn test` or `npm test`